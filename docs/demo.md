# Demo

Seed participants:

- Amara: English, Yoruba; Lagos, Nigeria
- Diego: Spanish, English; Medellin, Colombia

Seed room:

```text
How communities can preserve local knowledge and coordinate low-cost clean-water projects without outside organizations controlling the process.
```

The demo identity switcher is labeled `Development Demo Only`.

Run:

```bash
docker compose up --build
```

Open two browser windows at `http://localhost:5173`, choose different identities, and follow the flow in `README.md`.
